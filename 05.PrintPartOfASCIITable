using System;

namespace _05.PrintPartOfASCIITable
{
    class Program
    {
        static void Main(string[] args)
        {
            int startIndex = int.Parse(Console.ReadLine());
            int endtIndex = int.Parse(Console.ReadLine());
            for (int i = startIndex; i <= endtIndex; i++)
            {
                char c = (char)i;
                Console.Write($"{c} ");
            }
        }
    }
}
